function main() {
    const text = "neko";
    morphologicalAnalysis(neko).then(
        {
            //名詞かどうかの判定
            if() {
                mediaApi()
            }
        }
    ).then(

    );

}